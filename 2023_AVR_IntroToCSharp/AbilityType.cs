namespace AVR.Core
{
    //think of an enum as a data type (like an int) with a fixed set of options
    public enum AbilityType
    {
        Beginner,
        Intermediate,
        Expert,
        God
    }
}